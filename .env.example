# Porta do servidor (padrão: 3000)
PORT=3000

# Chave da API do Groq (gratuito em https://console.groq.com)
# Sem essa chave o sistema funciona normalmente, mas sem análise de IA
GROQ_API_KEY=sua_chave_groq_aqui